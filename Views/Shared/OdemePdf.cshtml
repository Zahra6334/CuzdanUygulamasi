<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Ödeme Taksitleri Listesi</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            font-size: 12px;
            color: #333;
            background-color: #f9f3f3;
            margin: 0;
            padding: 20px;
        }

        .container {
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(139, 0, 0, 0.1);
            padding: 25px;
            margin: 0 auto;
            max-width: 1000px;
        }

        h2 {
            text-align: center;
            margin-bottom: 25px;
            color: #8B0000;
            font-size: 22px;
            padding-bottom: 10px;
            border-bottom: 2px solid #8B0000;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            box-shadow: 0 2px 10px rgba(139, 0, 0, 0.05);
        }

        th {
            background-color: #8B0000;
            color: white;
            padding: 10px;
            text-align: center;
            font-weight: 600;
            letter-spacing: 0.5px;
        }

        td {
            padding: 10px;
            text-align: center;
            border-bottom: 1px solid #e0c9c9;
        }

        tr:nth-child(even) {
            background-color: #fff5f5;
        }

        tr:hover {
            background-color: #f8e8e8;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 1px solid #e0c9c9;
        }

        .logo {
            color: #8B0000;
            font-weight: bold;
            font-size: 18px;
        }

        .document-info {
            text-align: right;
            color: #666;
            font-size: 11px;
        }

        footer {
            margin-top: 30px;
            text-align: center;
            font-size: 10px;
            color: #8B0000;
            padding-top: 15px;
            border-top: 1px solid #e0c9c9;
        }

        .total-row {
            font-weight: bold;
            background-color: #f8e8e8 !important;
            color: #8B0000;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">ÖDEME TAKİP SİSTEMİ</div>
            <div class="document-info">
                Belge No: @DateTime.Now.ToString("yyyyMMddHHmm")<br>
                Tarih: @DateTime.Now.ToShortDateString()
            </div>
        </div>

        <h2>ÖDEME TAKSİTLERİ LİSTESİ</h2>

        <table>
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Ödenen Tutar (₺)</th>
                    <th>Ödeme Tarihi</th>
                    <th>Ödeme Yöntemi</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.OdenenTutar.ToString("N2")</td>
                        <td>@item.OdemeTarihi.ToShortDateString()</td>
                        <td>@(item.OdemeYontemi ?? "-")</td>
                    </tr>
                }
                
            </tbody>
        </table>

        <footer>
            Bu belge sistem tarafından @DateTime.Now.ToString("dd.MM.yyyy HH:mm") tarihinde otomatik üretilmiştir.<br>
            © @DateTime.Now.Year - Tüm hakları saklıdır.
        </footer>
    </div>
</body>
</html>